<?php

namespace App\Http\Controllers;

use App\Models\{{ $model }};
use Illuminate\Contracts\View\View;
use Illuminate\Http\RedirectResponse;
use Illuminate\Http\Request;

class {{ $controllerName }} extends Controller
{
    public function index(): View
    {
        return view('{{ $view }}.index');
    }

    public function create(): View
    {
        return view('{{ $view }}.create');
    }

    public function store(Request $request): RedirectResponse
    {
        return redirect()->route('{{ $name }}.index');
    }

    public function show({{ $model }} $model): View
    {
        return view('{{ $view }}.show', compact('model'));
    }

    public function edit({{ $model }} $model): View
    {
        return view('{{ $view }}.edit', compact('model'));
    }

    public function update({{ $model }} $model, Request $request): RedirectResponse
    {
        return redirect()->route('{{ $name }}.index');
    }

    public function delete({{ $model }} $model): RedirectResponse
    {
        return redirect()->route('{{ $name }}.index');
    }
}
